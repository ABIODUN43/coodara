// TEMP dev flags — flip these off once the corresponding backend endpoints
// are live/stable. Keeping them in one file means "going real" is a
// one-line change per feature, not a hunt through every page that reads it.

export const USE_MOCK_ARCHITECTURE_DATA = true;
export const USE_MOCK_ORGANIZATIONS_DATA = false;
export const USE_MOCK_REPOSITORIES_DATA = false;
export const USE_MOCK_ANALYSIS_DATA = true;